{% docs order_status %}
The current status of the order. Valid values:
- `placed` — order received, not yet fulfilled
- `shipped` — dispatched to carrier
- `delivered` — confirmed received by customer
- `cancelled` — voided before fulfillment
{% enddocs %}